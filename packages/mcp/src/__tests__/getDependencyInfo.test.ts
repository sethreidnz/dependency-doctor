import { describe, it, expect } from "vitest";
import getDependencyInfo from "../tools/getDependencyInfo";

describe("getDependencyInfo Tool", () => {
  it("should return the correct dependency information message", () => {
    // placeholder test for now
    expect(true).toBe(true);
  });
});
